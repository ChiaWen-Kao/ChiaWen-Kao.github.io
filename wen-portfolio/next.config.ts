import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "export",  // 02092025 Static export for GitHub pages
  basePath: "/wen-portfolio",  // 02092025 Base path for GitHub pages
};

export default nextConfig;
